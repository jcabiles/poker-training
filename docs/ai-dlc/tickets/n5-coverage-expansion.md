# N5 — coverage expansion · ticket DAG

Spec: `docs/ai-dlc/specs/n5-coverage-expansion.md` · Contracts: `docs/ai-dlc/contracts/n5-coverage-gaps.md`
Verify: `./scripts/verify.sh` · `cd backend && ruff check .` · `cd frontend && npm run typecheck && npm run build` (FE untouched, must stay green). One schema change (migration 0012).

| # | Ticket | Owned files | Depends | Done-condition |
|---|--------|-------------|---------|----------------|
| **T1** | Fixed-seed coverage baseline harness | new `backend/tests/test_coverage_baseline.py` (+ fixture JSON for the recorded number) | — | Deterministic domain-loop harness (fixed seed + fixed lineup + scripted hero, NO service/secrets RNG) reports graded/total; pre-N5 number recorded in the fixture; test asserts current ≥ recorded. Runs green on the unchanged tree first (this ticket lands the measuring stick BEFORE the movers). |
| **T2** | A1 flop-gate band + actual-open math | `app/domain/table/grade_map_postflop.py` (`map_flop_cbet` only), `backend/tests/test_sim_postflop_chart.py`/existing flop-cbet tests | T1 | Gate = `[2.0, _STD_OPEN_CAP]` band; pot check + small/big fractions keyed on `raises[0].amount_bb` (the ACTUAL open — refuter HIGH); belt test: 2.5bb open from a 3.0-canonical seat maps with pot/SPR/bets from 2.5; canonical opens byte-identical; N4b belt test (bot-rounded turn/river bets fire) added |
| **T3** | B: 9 blind-defense entries + nesting tests | `content/preflop/blind_defense.json`, new `backend/tests/test_blind_defense_nesting.py` | — | Research entries adopted verbatim (BB×4 call+raise, SB×5 raise-only, `sizing_bb: 10.0`); schema-valid; containment chains asserted (BB vs-UTG⊆…⊆vs-CO; SB vs-UTG⊆…⊆vs-BTN); advisory-frequency note honored (no freq logic); previously-graded spots byte-identical (new entries only ADD mappings) |
| **T4** | A3: spot-dims columns + migration 0012 | `app/db/models.py`, new `alembic/versions/0012_*.py`, `app/services/sim_session.py` (`_sim_decision_row` + call site only), migration test | — | Nullable `position`/`facing_position`/`players_in_pot`/`node_context` on `sim_decision`; `_sim_decision_row(spot: Spot \| None)` param; None-spot: position from state, rest null (refuter HIGH semantics); up/down/up green; existing rows/queries unaffected |
| **T5** | C5: `_apply_multiway` thin-value dampen + annotations | `app/domain/postflop.py` (`_apply_multiway` + comments), `backend/tests/test_multiway.py` | — | `_MW_THIN_VALUE_DAMPEN = 0.7` on aggressor-side weak_made small/big merits; HU-shaped-input annotations on `_villain_pos`/`range_advantage`/`_cbet_fold_equity_value`; new tests pin the branch; **enumerate Practice `players_in_pot>2` callers — if any live path serves MW drills, list + accept the grade shift explicitly in the PR** |
| **T6** | C1-C3: MW preflop/street gates + 3 mappers | `app/domain/table/grade_map_postflop.py` (new `_mw_srp_preflop`, MW street gate, `map_mw_flop_vs_cbet`/`map_mw_vs_turn_bet`/`map_mw_vs_river_bet`), `app/domain/table/grade_map.py`, new `backend/tests/test_grade_map_multiway.py` | T2, T3, T5 | 3-way SRP only (opener + hero-caller + BB, all IN); bettor==raiser at canonical size; hero CLOSES (all other defenders responded); ranges hero=VS_RFI+call, villain=RFI+raise, BB entry = gate-only (refuter HIGH — no phantom consumer); `players_in_pot=3` spots; two-RAISE legs reused; still-None matrix asserted (limped/donk/4-way/non-closing/checked-to caller/MW 3-bet pots); signature pins green ("mw" dim distinct, HU hashes byte-identical) |
| **T7** | C6-C7: dispatcher + display + e2e | `app/services/sim_session.py` (`_facing_raise_spot` extension), `backend/tests/test_sim_postflop_sizing.py` (extend) | T4, T6 | MW mappers in dispatcher after HU chain (disjoint by live-count, verified); two-size display reads legs off the MW spot (parity); e2e: 3-way hand → graded `SimDecision` persists with `players_in_pot=3` + sizing verdict at an MW facing node |
| **T8** | Verify + coverage delta + design-review | — (review only) | T1–T7 | Full verify green; T1 harness shows post-N5 ≥ pre-N5 (record delta in slice notes); enumerated newly-mapping list asserted one-by-one; refuter-on-diff; design-review (coach mode, both themes) at an MW facing node |

## Sequencing
**T1 first** (baseline before movers). Then **T2 ‖ T3 ‖ T4 ‖ T5** (disjoint files). Then **T6 → T7 → T8**. `grade_map_postflop.py` owned by T2 then T6 (sequential); `sim_session.py` by T4 then T7 (sequential); `postflop.py` solely T5.

## Acceptance highlights
- T2 carries refuter HIGH-1 (actual-open math, not just gate relax). T4 carries HIGH-3 (spot param + None semantics). T6 carries HIGH-2 (BB range gate-only).
- T5 lands BEFORE T6 so the dampen change never alters live-reachable output mid-slice.
- Strict superset everywhere: existing graded flows byte-identical; N5 only ADDS mapped spots (+ the T5 MW-gated change whose live callers are enumerated, not assumed zero).
